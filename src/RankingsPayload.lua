-- Bulk rankings from the Native Uploader live here (same file is overwritten on each Send).
-- This is not SavedVariables, so WoW will not save stale in-memory rankings over an external update on /reload.
WowLogsRankingsPayload = {
  updatedAt = 0,
  players = {},
}
